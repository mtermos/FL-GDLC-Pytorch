# MLP model configuration
model:
  name: "mlp"
  type: "mlp"

layers:
- dense

input_layer_norm: false

dense:
  # units: [200, 80]
  units: [400, 200, 80]
  # units: [ 80 ]
  activation: "leaky_relu"
  dropout: true
  dropout_rate: 0.2
  batch_norm: false
  layer_norm: true
